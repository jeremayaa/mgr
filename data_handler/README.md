In nbia_downloader there are two classes:

MetaDataCollector (initialized with num_workers) - its main method get_collection_metadata(collection_name) saves a JSON file with metadata under specified path

DataDownloader